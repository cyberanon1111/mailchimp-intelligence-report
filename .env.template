# Copy to .env for local runs. In GitHub Actions, these are set as repository
# secrets instead (see README.md) — never committed here.

# ── Mailchimp ─────────────────────────────────────────────────
MAILCHIMP_API_KEY=
MAILCHIMP_SERVER=us22
MAILCHIMP_AUDIENCE_ID=

# ── Report email ──────────────────────────────────────────────
REPORT_RECIPIENTS=arjun@maps2business.com,admin@maps2business.com
REPORT_FROM_EMAIL=marketing@maps2business.com

SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=true

# Safety switch — while true, reports generate normally but no email is sent.
TEST_MODE=true
